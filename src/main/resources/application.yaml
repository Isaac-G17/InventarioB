spring:
  application:
    name: app
  datasource:
    url: jdbc:h2:mem:inventario
    username: root
    password:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
security:
  jwt:
    secret: demo-secret-key-change-this-in-production-123456789
    access-token-expiration-ms: 900000
    refresh-token-expiration-ms: 604800000